{
  "name": "next-pkg-common",
  "version": "1.0.0",
  "description": "demo",
  "private": true,
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "clean": "rm -rf build .tscache",
    "build": "tsc --build .",
    "start": "yarn build --watch --preserveWatchOutput"
  },
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "typescript": "^4.6.3"
  }
}
